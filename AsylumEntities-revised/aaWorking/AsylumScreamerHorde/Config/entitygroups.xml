<?xml version="1.0" encoding="UTF-8"?>
<configs>

<!-- Asylum Screamer Horde (Izzi Jones) - Entity Group Spawning -->
<!-- Generated: 2025-12-01 15:40:40 -->
<!--
    This file adds asylumScreamerHorde to screamer spawn groups.
    Izzi Jones will now spawn alongside regular screamers.
-->

    <!-- NEW: Entity group for the invisible spawner entity used by SpawnEntitySDX -->
    <append xpath="/entitygroups">
        <entitygroup name="WanderingAndScreamerHordeSpawnerGroup">
            <entity name="WanderingAndScreamerHordeSpawner" prob="1.0"/>
        </entitygroup>
    </append>

    <!-- ZombieScouts (existing entities: 0) -->
    <append xpath="/entitygroups/entitygroup[@name='ZombieScouts']">
        <entity name="asylumScreamerHorde" prob="1.0"/>
    </append>

    <!-- ZombieScoutsFeral (existing entities: 0) -->
    <append xpath="/entitygroups/entitygroup[@name='ZombieScoutsFeral']">
        <entity name="asylumScreamerHorde" prob="1.0"/>
    </append>

    <!-- ZombieScoutsRadiated (existing entities: 0) -->
    <append xpath="/entitygroups/entitygroup[@name='ZombieScoutsRadiated']">
        <entity name="asylumScreamerHorde" prob="1.0"/>
    </append>

    <!-- scoutHordeStageGS1 (existing entities: 0) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1']">
        <entity name="asylumScreamerHorde" prob="1.0"/>
    </append>

    <!-- scoutHordeStageGS1000 (existing entities: 13) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1000']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1015 (existing entities: 14) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1015']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS103 (existing entities: 8) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS103']">
        <entity name="asylumScreamerHorde" prob="0.75"/>
    </append>

    <!-- scoutHordeStageGS1030 (existing entities: 12) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1030']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1045 (existing entities: 11) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1045']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1060 (existing entities: 17) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1060']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1075 (existing entities: 17) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1075']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1091 (existing entities: 9) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1091']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS11 (existing entities: 0) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS11']">
        <entity name="asylumScreamerHorde" prob="1.0"/>
    </append>

    <!-- scoutHordeStageGS110 (existing entities: 7) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS110']">
        <entity name="asylumScreamerHorde" prob="0.79"/>
    </append>

    <!-- scoutHordeStageGS1106 (existing entities: 17) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1106']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1122 (existing entities: 15) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1122']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1137 (existing entities: 10) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1137']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1153 (existing entities: 10) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1153']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1169 (existing entities: 15) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1169']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS117 (existing entities: 12) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS117']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1185 (existing entities: 11) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1185']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1201 (existing entities: 11) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1201']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1217 (existing entities: 12) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1217']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1233 (existing entities: 16) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1233']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1249 (existing entities: 8) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1249']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS125 (existing entities: 10) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS125']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1265 (existing entities: 17) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1265']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1281 (existing entities: 15) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1281']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1298 (existing entities: 11) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1298']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1314 (existing entities: 15) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1314']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS132 (existing entities: 8) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS132']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1331 (existing entities: 12) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1331']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1347 (existing entities: 16) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1347']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1364 (existing entities: 7) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1364']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1380 (existing entities: 11) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1380']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1397 (existing entities: 13) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1397']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS14 (existing entities: 0) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS14']">
        <entity name="asylumScreamerHorde" prob="1.0"/>
    </append>

    <!-- scoutHordeStageGS140 (existing entities: 10) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS140']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1414 (existing entities: 11) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1414']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1431 (existing entities: 11) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1431']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1448 (existing entities: 15) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1448']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1465 (existing entities: 12) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1465']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS148 (existing entities: 10) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS148']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1482 (existing entities: 11) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1482']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1499 (existing entities: 8) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1499']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1516 (existing entities: 9) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1516']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1533 (existing entities: 15) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1533']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1551 (existing entities: 18) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1551']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS156 (existing entities: 6) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS156']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1568 (existing entities: 8) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1568']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1586 (existing entities: 12) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1586']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1603 (existing entities: 8) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1603']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1621 (existing entities: 9) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1621']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1638 (existing entities: 9) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1638']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS164 (existing entities: 12) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS164']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1656 (existing entities: 8) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1656']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1674 (existing entities: 10) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1674']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1692 (existing entities: 13) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1692']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1710 (existing entities: 8) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1710']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS172 (existing entities: 9) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS172']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1728 (existing entities: 11) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1728']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1746 (existing entities: 12) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1746']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1764 (existing entities: 8) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1764']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1782 (existing entities: 14) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1782']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS18 (existing entities: 0) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS18']">
        <entity name="asylumScreamerHorde" prob="1.0"/>
    </append>

    <!-- scoutHordeStageGS1800 (existing entities: 11) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1800']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS181 (existing entities: 10) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS181']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1818 (existing entities: 13) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1818']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1837 (existing entities: 10) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1837']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1855 (existing entities: 12) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1855']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1873 (existing entities: 15) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1873']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS189 (existing entities: 9) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS189']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1892 (existing entities: 10) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1892']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1911 (existing entities: 13) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1911']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1929 (existing entities: 9) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1929']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1948 (existing entities: 11) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1948']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1967 (existing entities: 12) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1967']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS198 (existing entities: 10) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS198']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS1986 (existing entities: 10) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS1986']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS2 (existing entities: 0) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS2']">
        <entity name="asylumScreamerHorde" prob="1.0"/>
    </append>

    <!-- scoutHordeStageGS2004 (existing entities: 15) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS2004']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS2023 (existing entities: 15) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS2023']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS2042 (existing entities: 12) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS2042']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS2061 (existing entities: 11) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS2061']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS207 (existing entities: 11) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS207']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS2081 (existing entities: 12) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS2081']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS2100 (existing entities: 10) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS2100']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS2119 (existing entities: 12) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS2119']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS2138 (existing entities: 9) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS2138']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS2158 (existing entities: 10) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS2158']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS216 (existing entities: 11) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS216']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS2177 (existing entities: 12) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS2177']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS2197 (existing entities: 13) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS2197']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS22 (existing entities: 0) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS22']">
        <entity name="asylumScreamerHorde" prob="1.0"/>
    </append>

    <!-- scoutHordeStageGS2216 (existing entities: 14) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS2216']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS2236 (existing entities: 10) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS2236']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS225 (existing entities: 11) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS225']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS2255 (existing entities: 11) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS2255']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS2275 (existing entities: 9) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS2275']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS2295 (existing entities: 11) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS2295']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS2315 (existing entities: 10) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS2315']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS2334 (existing entities: 14) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS2334']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS234 (existing entities: 12) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS234']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS2354 (existing entities: 10) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS2354']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS2374 (existing entities: 11) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS2374']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS2394 (existing entities: 11) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS2394']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS2414 (existing entities: 12) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS2414']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS243 (existing entities: 9) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS243']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS2435 (existing entities: 8) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS2435']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS2455 (existing entities: 10) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS2455']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS2475 (existing entities: 9) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS2475']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS2495 (existing entities: 14) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS2495']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS2516 (existing entities: 18) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS
">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS512 (existing entities: 11) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS512']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS5144 (existing entities: 10) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS5144']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS5170 (existing entities: 15) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS5170']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS5196 (existing entities: 12) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS5196']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS52 (existing entities: 5) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS52']">
        <entity name="asylumScreamerHorde" prob="0.33"/>
    </append>

    <!-- scoutHordeStageGS5222 (existing entities: 9) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS5222']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS524 (existing entities: 15) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS524']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS5248 (existing entities: 11) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS5248']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS5274 (existing entities: 15) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS5274']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS5300 (existing entities: 10) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS5300']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS5326 (existing entities: 14) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS5326']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS5352 (existing entities: 9) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS5352']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS536 (existing entities: 13) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS536']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS5379 (existing entities: 10) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS5379']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS5405 (existing entities: 9) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS5405']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS5431 (existing entities: 14) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS5431']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS5458 (existing entities: 15) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS5458']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS548 (existing entities: 12) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS548']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS5484 (existing entities: 17) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS5484']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS5511 (existing entities: 9) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS5511']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS5537 (existing entities: 13) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS5537']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS5564 (existing entities: 7) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS5564']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS5590 (existing entities: 11) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS5590']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS560 (existing entities: 12) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS560']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS5617 (existing entities: 11) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS5617']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS5644 (existing entities: 15) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS5644']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS5670 (existing entities: 13) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS5670']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS5697 (existing entities: 10) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS5697']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS5724 (existing entities: 16) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS5724']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS573 (existing entities: 20) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS573']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS5751 (existing entities: 14) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS5751']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS5778 (existing entities: 11) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS5778']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS58 (existing entities: 4) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS58']">
        <entity name="asylumScreamerHorde" prob="0.35"/>
    </append>

    <!-- scoutHordeStageGS585 (existing entities: 13) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS585']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS598 (existing entities: 14) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS598']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS610 (existing entities: 10) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS610']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS623 (existing entities: 10) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS623']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS636 (existing entities: 13) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS636']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS64 (existing entities: 6) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS64']">
        <entity name="asylumScreamerHorde" prob="0.41"/>
    </append>

    <!-- scoutHordeStageGS649 (existing entities: 9) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS649']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS662 (existing entities: 16) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS662']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS675 (existing entities: 15) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS675']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS688 (existing entities: 8) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS688']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS70 (existing entities: 7) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS70']">
        <entity name="asylumScreamerHorde" prob="0.48"/>
    </append>

    <!-- scoutHordeStageGS702 (existing entities: 11) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS702']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS715 (existing entities: 14) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS715']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS729 (existing entities: 10) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS729']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS742 (existing entities: 12) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS742']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS756 (existing entities: 16) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS756']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS76 (existing entities: 8) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS76']">
        <entity name="asylumScreamerHorde" prob="0.48"/>
    </append>

    <!-- scoutHordeStageGS769 (existing entities: 10) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS769']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS783 (existing entities: 12) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS783']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS797 (existing entities: 9) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS797']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS8 (existing entities: 0) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS8']">
        <entity name="asylumScreamerHorde" prob="1.0"/>
    </append>

    <!-- scoutHordeStageGS811 (existing entities: 13) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS811']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS82 (existing entities: 8) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS82']">
        <entity name="asylumScreamerHorde" prob="0.53"/>
    </append>

    <!-- scoutHordeStageGS825 (existing entities: 10) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS825']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS839 (existing entities: 15) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS839']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS853 (existing entities: 18) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS853']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS868 (existing entities: 13) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS868']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS882 (existing entities: 8) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS882']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS89 (existing entities: 11) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS89']">
        <entity name="asylumScreamerHorde" prob="0.59"/>
    </append>

    <!-- scoutHordeStageGS896 (existing entities: 14) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS896']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS911 (existing entities: 12) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS911']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS925 (existing entities: 13) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS925']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS940 (existing entities: 16) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS940']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS955 (existing entities: 14) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS955']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS96 (existing entities: 6) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS96']">
        <entity name="asylumScreamerHorde" prob="0.65"/>
    </append>

    <!-- scoutHordeStageGS970 (existing entities: 14) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS970']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- scoutHordeStageGS985 (existing entities: 13) -->
    <append xpath="/entitygroups/entitygroup[@name='scoutHordeStageGS985']">
        <entity name="asylumScreamerHorde" prob="0.8"/>
    </append>

    <!-- zombieScreamer (existing entities: 0) -->
    <append xpath="/entitygroups/entitygroup[@name='zombieScreamer']">
        <entity name="asylumScreamerHorde" prob="1.0"/>
    </append>

    <!-- zombieScreamerCharged (existing entities: 0) -->
    <append xpath="/entitygroups/entitygroup[@name='zombieScreamerCharged']">
        <entity name="asylumScreamerHorde" prob="1.0"/>
    </append>

    <!-- zombieScreamerFeral (existing entities: 0) -->
    <append xpath="/entitygroups/entitygroup[@name='zombieScreamerFeral']">
        <entity name="asylumScreamerHorde" prob="1.0"/>
    </append>

    <!-- zombieScreamerRadiated (existing entities: 0) -->
    <append xpath="/entitygroups/entitygroup[@name='zombieScreamerRadiated']">
        <entity name="asylumScreamerHorde" prob="1.0"/>
    </append>

</configs>